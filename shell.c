#include "shell.h"

/**
 * main - main function of the shell.
 * @argc: number of arguments.
 * @argv: arguments passed.
 *
 * Description: main function of the shell.
 * Return: Always 0.
 */

int main(__attribute__((unused)) int argc,
	 __attribute__((unused)) char **argv)
{

	_loop();

	return (0);
}
